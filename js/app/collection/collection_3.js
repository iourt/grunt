define([
	'jquery',
	'underscore',
	'backbone'
], function($, _, Backbone){
	var Collection3 = function(){
		// 集合类提供了3个方法用于从集合中移除模型对象，分别是：
		 
		// remove()：从集合中移除一个或多个指定的模型对象
		// pop()：移除集合尾部的一个模型对象
		// shift()：移除集合头部的一个模型对象

		
	};
	return Collection3;
});
